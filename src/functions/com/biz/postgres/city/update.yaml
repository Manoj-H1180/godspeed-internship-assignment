summary: Update City
tasks:
  - id: postgres_city_update
    fn: com.gs.datastore
    args:
      datasource: postgres
      data:
        where:
          id: <% parseInt(inputs.params.id) %>
        data: <% inputs.body %>
      config:
        method: City.update
